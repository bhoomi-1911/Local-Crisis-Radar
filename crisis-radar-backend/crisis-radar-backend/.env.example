PORT=4000

# Local Postgres connection string — adjust user/password/db name to match your setup
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/crisis_radar

# Use a long random string in real use — this is fine for local dev only
JWT_SECRET=replace-this-with-a-long-random-string
JWT_EXPIRES_IN=7d
